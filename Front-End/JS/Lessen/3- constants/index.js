
// const is a constant and it can't be changed. As so, we'll get an error.
const interestRate = 0.3;
interestRate = 1;
console.log(interestRate);

// line 4 has an error because the constant cannot be changed.